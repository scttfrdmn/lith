# Build artifacts
/bin/
/dist/
*.exe
*.test
*.out
coverage.*

# Local index files and caches
*.lith
*.lithidx
/cache/

# Host-specific NIC baseline cache (code writes nic-<uid>.json)
nic*.json

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
site/

# Built binary (produced by `go build`; the release binary comes from goreleaser)
/lith
